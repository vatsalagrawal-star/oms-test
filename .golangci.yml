# .golangci.yml
version: "2"

run:
  timeout: 3m

linters:
  enable:
    - staticcheck
    - errcheck
    - govet
    - ineffassign
    - gosec
    - misspell

issues:
  exclude-use-default: false

linters-settings:
  misspell:
    locale: US
